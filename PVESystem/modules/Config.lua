local Config = {
    Enabled = true,
    MaxDistance = 1000,
    Colors = {
        NPC = Color3.fromRGB(255, 0, 0), -- Red for NPCs
        Default = Color3.fromRGB(200, 200, 200), -- Gray for tracked items
    }
}

return Config
